name: soft56
description: Windows 桌面应用
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.4.1
  system_tray: ^2.0.3  # 这里修复了！
  path_provider: ^2.1.1
  file_picker: ^6.1.1

flutter:
  uses-material-design: true
