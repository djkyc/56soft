name: soft56
description: Windows 桌面应用
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.4.0
  system_tray: ^0.2.0
  path_provider: ^2.1.0
  file_picker: ^6.0.0

flutter:
  uses-material-design: true
